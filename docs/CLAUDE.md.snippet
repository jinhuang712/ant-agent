<!--
把下面这节并进你的 ~/.claude/CLAUDE.md（或项目级 CLAUDE.md）。节号按你自己的文件顺延。
没有这一节的话 Claude 默认不会主动派 sub agent——Opus 的系统提示里带着一句
「用户没要求就别调 AgentTool」，这节就是那句要的「用户要求」。
-->

## N. 一次性调查一律派蚂蚁

**读代码、核事实、捞证据、跑命令、等终态这类活，一律 spawn 蚂蚁去干，主会话只做编排 · 分析 · 决策 · 问答。** 这是我的显式要求，每个 session 都算，不用逐次问，也不用汇报「我要开个 agent 了」。

目的是**压缩比**：同一件事主会话自己干要读几万 token 把上下文塞满，而真实答案往往只有一行。让便宜模型去趟那几万 token，只把那一行带回来。

### 派发和收活都是静默的

**派的时候一句话**：「派个 `ant-sift` 去扫远端。」不解释为什么认领这只、不列排除了哪几只、不贴 dispatch 里写了什么约束。

**多只并行就等到齐。** 一只先回不是说话的理由。中途被唤醒而其他还在跑，就不写面向用户的段落——「某条证据链已经闭合，等另一只回来再一次性给结论」这种句子占了一次阅读却没给任何东西。这是实测里最普遍的缺陷：六个 session 全中，约 24 次。

**收的时候只给结论。** 蚂蚁的返回是原料不是产物——原样转贴等于把压缩掉的东西倒回主线，压缩比归零。核 pin 照做但不汇报，核不过才说。

回复里只留两样：**结论**，和**真正需要我拍板的决策项**。

### 八只，按认知状态认领

| ant | 我知道 | 我不知道 | model |
|---|---|---|---|
| `ant-locate` | 它是什么 | 它在哪 | sonnet |
| `ant-verify` | 它在哪 | 它的值是什么 | haiku |
| `ant-trace` | 两头 | 中间怎么连的 | sonnet |
| `ant-sift` | 什么算有用 | 有用的有哪些 | sonnet |
| `ant-census` | 我想要一个数 | 这数多少，以及什么算一个 | sonnet |
| `ant-adjust` | 每一步怎么做 | — | 按活选 |
| `ant-pardon` | 每一步怎么做，且不打算回看 | — | haiku |
| `ant-monitor` | 终态长什么样 | 什么时候到 | haiku |

**这张表是认领时唯一的对照物。** 对象是代码、配置、日志还是线上数据，不参与判断；手段是查、算、跑还是改，也不参与。

### 动手前的决策流

```
这一步能用一条确定性命令可靠回答吗？
  能 ──→ 自己跑，到此为止（wc -c 不会数错，模型会）
  不能 ↓
这一步只是「把某个事实找出来 / 把某件事做掉」吗？
  不是——我在决策 / 设计 / 写文档正文 / 跟用户对话 ──→ 自己做
  是 ──→ 对照上面那张表认领
```

**唯一的例外**：单条 + 已精确到「文件:行号」 + 主上下文还宽裕，自己看一眼更快。三条缺一不可，上下文一满即失效。

主会话保留：改代码（除非明确授权给 `ant-adjust`）· 决策 · 设计 · 分析 · 写文档正文 · 跟我对话 · 读文档正文 · 照 pin 核实。

### 四条纪律

- **dispatch 时必须显式传 `model`。** frontmatter 里的 `model` 实测不生效，不传就继承主会话的 opus，降档收益全丢
- **agent 的 result 不是完成证据。** 照它给的 pin 回原始现场比对原文——核 pin，不核推理
- **先看 `Unplanned` 那一栏。** 它装的是蚂蚁撞见但我没问的东西，往往比我问的那部分更值钱
- **符号导航类 MCP 的 active project 是进程级状态。** 同仓可并行多只，**跨仓必须串行**

### 写授权

`ant-adjust` 和 `ant-pardon` 默认只读，副作用要在 dispatch 时显式授权。

`ant-pardon` 风险最高——有写权限而且我不看结果。派它之前过三条：**过程确定 · 难搞错 · 可事后验**，缺一条改派 `ant-adjust`。不可逆的活一律不派它。
